on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    uses: ./.github/workflows/test.yml

  save-pages-cache:
    needs: [test]
    uses: ./.github/workflows/save-pages-cache.yml

  deploy-pages:
    needs: [save-pages-cache]
    uses: ./.github/workflows/deploy-pages.yml
